See `AGENTS.md` in this same directory — that file has the toolchain, run/test commands, and
conventions for working on this project. It's kept tool-agnostic on purpose so it's useful
regardless of which agent or editor you're using.
